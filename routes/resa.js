var express = require('express');
var router = express.Router();

/* GET home page. */
router.get('/resa', function(req, res, next) {
  // verify credentials

  // get db call

  // return auth data

});

module.exports = router;
