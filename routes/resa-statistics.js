var express = require('express');
var router = express.Router();

/* GET home page. */
router.get('/', function(req, res, next) {
  // get db call

  // do maths and calculate statistics

  // return statistics

});

module.exports = router;
